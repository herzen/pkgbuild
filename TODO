 - ability to build from CVS
 - generate plain specs and CVS snapshot tarballs
 - wrap rpm on Linux
 - check versions in dependencies (done, needs testing, bugfixing)
 - Solaris patch dependency
 - %{?foo}, %ifosrel
 - %files -f
 - %(command)
 - %config (missing_ok)

--

Bugs:

 ? complains about dependencies on packages defined in the same spec
 + does not correctly add dependencies on pkgs defined in the same spec
 + evaluates comment lines
 + empty lines in %files break parsing

 Key:
  ? not confirmed/could not reproduce
  + recently fixed
  - dispatched
